[project]
name = "ai-agent-writing-book-template"
version = "0.1.0"
description = "Build a Japanese technical book from Markdown with Pandoc and Typst"
requires-python = ">=3.10"
dependencies = [
    "pyyaml>=6.0,<7",
]

[tool.uv]
package = false
